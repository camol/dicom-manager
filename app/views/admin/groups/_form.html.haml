= simple_form_for [:admin, @group], :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :description
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to "Cancel", admin_groups_path, :class => 'btn'


