.container-fluid
  = simple_form_for @project, :html => { :class => 'form-horizontal' } do |f|
    .row-fluid
      .span6
        = f.input :name
        = f.input :description
        = f.button :submit, :class => 'btn-primary'
      .span6
        = f.association :groups, as: :check_boxes, label: "Assigned groups:", wrapper_html: { class: 'my-checkboxes' }



